/**
 * Token types used for lexems identified to be inserted into the Symbol table
*/
enum keywords
{
  INT=100
};

enum identifiers
{
  ID=200
};

enum punctuators
{
  DELIMITER=200
};

enum operators
{
  INCRECEMENT=300
};

enum constants
{
  INT_CONST=400,
  STRING_CONST
};

